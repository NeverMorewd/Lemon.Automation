<Page
    x:Class="Lemon.Automation.App.UITracker.Views.TestView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Lemon.Automation.App.UITracker.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="TestView"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid>
        <StackPanel>
            <ui:TextBox Name="textBox" PlaceholderText="Type something to set clipboard" />
            <ui:CardAction
                Command="{Binding TestClipboardCommand}"
                CommandParameter="{Binding ElementName=textBox, Path=Text}"
                Content="TestClipboard"
                Icon="{ui:SymbolIcon Home24}" />
            <ui:CardAction
                Command="{Binding BrowseExeAndRunCommand}"
                Content="BrowseExeAndRun"
                Icon="{ui:SymbolIcon Home24}" />
            <Button Content="Test"  Command="{Binding TestCommand}"/>
        </StackPanel>

    </Grid>
</Page>
