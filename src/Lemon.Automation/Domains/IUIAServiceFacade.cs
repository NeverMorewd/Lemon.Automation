using R3;

namespace Lemon.Automation.Domains
{
    public interface IUIAServiceFacade
    {
        //IUIAElement ElementFromCurrentPoint(bool isEnableDeepTraversal);

        //Observable<IUIAElement> ObserveElementsFromCurrentPoint(TimeSpan interval,
        //    bool isEnableDeepTraversal,
        //    CancellationToken cancellationToken);

        //Observable<IUIAElement> ObserveElementsByMouseMove(TimeSpan interval,
        //    bool isEnableDeepTraversal,
        //    CancellationToken cancellationToken);

        //IEnumerable<IUIAElement> GetAllChildFromPoint();

        //IUIAElement GetClosestAndDeepestChildFromPoint();

        //IUIAElement GetDesktop();

        //IEnumerable<IUIAElement> GetAllChildren(IUIAElement uiElement);


    }
}
